<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ns="clr-namespace:OpenTabletDriverGUI.Views;assembly=OpenTabletDriverGUI"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="OpenTabletDriverGUI.Views.AreaSetter">
    <UserControl.Styles>
        <Style Selector="Border">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="CornerRadius" Value="5"/>
        </Style>
        <Style Selector="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>
        <Style Selector="TextBlock.UnitLabel">
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
        </Style>
        <Style Selector="TextBox">
            <Setter Property="Margin" Value="1"/>
            <Setter Property="TextAlignment" Value="Left"/>
        </Style>
    </UserControl.Styles>
    <Grid RowDefinitions="Auto,*,Auto" ColumnDefinitions="*,*,*,*">
        <TextBlock Grid.ColumnSpan="4" Classes="h1" Text="{Binding $parent[ns:AreaSetter].Title}"/>
        <Border Grid.ColumnSpan="4" Grid.Row="1" HorizontalAlignment="Center" Background="Transparent">
            <Viewbox HorizontalAlignment="Center">
                <Canvas Width="{Binding $parent[ns:AreaSetter].BackgroundWidth}" Height="{Binding $parent[ns:AreaSetter].BackgroundHeight}" HorizontalAlignment="Center">
                    <Rectangle Name="FullAreaRect" 
                        Width="{Binding $parent[ns:AreaSetter].BackgroundWidth}" Height="{Binding $parent[ns:AreaSetter].BackgroundHeight}"
                        Fill="#B6B6B6"/>
                    <Rectangle Name="ActiveAreaRect"
                        Width="{Binding $parent[ns:AreaSetter].AreaWidth}" Height="{Binding $parent[ns:AreaSetter].AreaHeight}"
                        Canvas.Left="{Binding $parent[ns:AreaSetter].AreaXOffset}" Canvas.Top="{Binding $parent[ns:AreaSetter].AreaYOffset}"
                        Fill="{DynamicResource FocusedBrush}"/>
                </Canvas>
            </Viewbox>
        </Border>
        <Border Grid.Column="0" Grid.Row="2">
            <Grid RowDefinitions="*,Auto">
                <TextBlock Grid.Row="0" Text="Width"/>
                <TextBox Grid.Row="1" Text="{Binding $parent[ns:AreaSetter].AreaWidth}"/>
                <TextBlock Grid.Row="1" Classes="UnitLabel" Text="{Binding $parent[ns:AreaSetter].MeasurementUnit}"/>
            </Grid>
        </Border>
        <Border Grid.Column="1" Grid.Row="2">
            <Grid RowDefinitions="*,Auto">
                <TextBlock Grid.Row="0" Text="Height"/>
                <TextBox Grid.Row="1" Text="{Binding $parent[ns:AreaSetter].AreaHeight}"/>
                <TextBlock Grid.Row="1" Classes="UnitLabel" Text="{Binding $parent[ns:AreaSetter].MeasurementUnit}"/>
            </Grid>
        </Border>
        <Border Grid.Column="2" Grid.Row="2">
            <Grid RowDefinitions="*,Auto">
                <TextBlock Grid.Row="0" Text="X Offset"/>
                <TextBox Grid.Row="1" Text="{Binding $parent[ns:AreaSetter].AreaXOffset}"/>
            </Grid>
        </Border>
        <Border Grid.Column="3" Grid.Row="2">
            <Grid RowDefinitions="*,Auto">
                <TextBlock Grid.Row="0" Text="Y Offset"/>
                <TextBox Grid.Row="1" Text="{Binding $parent[ns:AreaSetter].AreaYOffset}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
